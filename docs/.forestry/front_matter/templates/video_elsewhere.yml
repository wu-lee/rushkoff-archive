---
label: Video (Elsewhere)
hide_body: false
fields:
- type: text
  name: title
  label: title
  config:
    required: true
- type: datetime
  name: date
  label: date
  config:
    required: true
- type: text
  name: video_url
  label: video_url
  config:
    required: true
- type: textarea
  name: notes
  label: notes
  config:
    wysiwyg: true
    schema:
      format: markdown
- name: categories
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: categories
  label: categories
