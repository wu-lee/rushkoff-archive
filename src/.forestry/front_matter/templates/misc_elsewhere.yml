---
label: Misc (Elsewhere)
hide_body: false
fields:
- type: text
  name: title
  label: title
  config:
    required: true
- name: image
  type: file
  config:
    maxSize: 64
  label: image
- type: datetime
  name: date
  label: date
  config:
    required: true
- type: textarea
  name: notes
  label: notes
  config:
    wysiwyg: true
    schema:
      format: markdown
- name: categories
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: categories
  label: categories
